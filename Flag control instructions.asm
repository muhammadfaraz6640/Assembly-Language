clc  ;clear carry flag to 0
stc  ;set carry flag to 1
cmc ;reverse the carry flag 

cld  ;clear direction flag to 0
std  ;set direction flag to 1 

cli ;clear interrupt flag to 0
sti ;set interrupt flag to 0

