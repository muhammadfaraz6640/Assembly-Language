main proc
    mov ax,02   ;move 02 to the ax register
    mov cx,ax   ;move then ax value which is 02 to the cx                                    
    ;instruction pointer increment every time because it holds the address of next instruction to be fetched