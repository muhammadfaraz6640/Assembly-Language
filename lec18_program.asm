main proc
    mov ax,0123        ;immediate addressing mode
    mov bx,0150         ;immediate addressing mode
    add ax,bx           ;register addressing mode
    inc bx             ;register addressing mode 
    dec ax             ;register addressing mode
    and ax,bx          ;register addressing mode
    mov [4210],ax      ;direct addressing mode